def calculate_power(base,power):
    result=base**power
    print(f"{base} raised to the {power} = {result}")
val_1=float(input("Enter Base: "))
val_2=float(input("Enter Power : "))
calculate_power(val_1,val_2)
