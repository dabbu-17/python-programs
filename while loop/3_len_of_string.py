text = "Python"
index = 0
length = 0

while index < len(text):
    print(text[index])  # Print each character on a new line
    index += 1  # Increment the index
    length += 1  # Increment the length count
print(f"Length of the string: {length}")
